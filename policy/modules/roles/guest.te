
policy_module(guest, 1.1.0)

########################################
#
# Declarations
#

role guest_r;

userdom_restricted_user_template(guest)

########################################
#
# Local policy
#

# This looks like a bad idea. Maybe make it tunable?
# Or make httpd_user types userdom_user_home_content.

# optional_policy(`
#	apache_role(guest_r, guest_t)
# ')

gen_user(guest_u, user, guest_r, s0, s0)
